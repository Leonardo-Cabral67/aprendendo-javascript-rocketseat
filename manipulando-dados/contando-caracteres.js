// Como contar caracteres 
let word = "paralelepípedo"
console.log(word.length)

// Contando dígitos

let number = 12345678
console.log(String(number).length)

// Aqui tive que transformar o número em string para poder usar a propriedade length