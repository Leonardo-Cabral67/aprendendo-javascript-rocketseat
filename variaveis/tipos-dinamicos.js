/* O Javascript é uma linguagem fracamente tipada. Isso quer dizer que : As variáveis declaradas não precisam ter
um tipo previamente definido. 

* É uma linguagem dinâmica.


Tipos de variáveis: 
    * var
    * let
    * const 
    
*/
let clima = true;

console.log(typeof clima);
